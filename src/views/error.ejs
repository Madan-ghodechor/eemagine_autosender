<%- include('partials/head', { title: 'Error' }) %>
<%- include('partials/navbar', { page: '' }) %>

<div class="container" style="max-width:520px;text-align:center;padding-top:80px;">
  <div style="font-size:64px;margin-bottom:16px;">⚠️</div>
  <h1 class="page-title" style="margin-bottom:10px;"><%= status || 'Error' %></h1>
  <p style="color:var(--muted);margin-bottom:28px;"><%= message || 'Something went wrong.' %></p>
  <a href="/" class="btn btn-primary">← Back to Upload</a>
</div>

<%- include('partials/footer') %>
